@inherits LayoutComponentBase

<MudThemeProvider />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<a href="#main-content" class="skip-nav">Skip to main content</a>

<MudLayout>
    <MudAppBar Elevation="1" Color="Color.Primary">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit"
                       Edge="Edge.Start" OnClick="@(() => _drawerOpen = !_drawerOpen)"
                       aria-label="Toggle navigation menu" />
        <MudText Typo="Typo.h6" Class="ml-3">Clinic Scheduler</MudText>
        <MudSpacer />
        <MudText Typo="Typo.body2" Color="Color.Inherit" Class="mr-4">
            @DateTime.Now.ToString("dddd, MMMM d yyyy")
        </MudText>
        <MudIconButton Icon="@Icons.Material.Filled.Chat" Color="Color.Inherit" 
                       OnClick="@(() => _chatDrawerOpen = !_chatDrawerOpen)" 
                       aria-label="Toggle AI Assistant" />
    </MudAppBar>

    <MudDrawer @bind-Open="_drawerOpen" Variant="DrawerVariant.Persistent"
               ClipMode="DrawerClipMode.Always" Elevation="2"
               role="navigation" aria-label="Main navigation">
        <MudDrawerHeader>
            <MudText Typo="Typo.subtitle1"><b>Pain Management</b></MudText>
        </MudDrawerHeader>
        <NavMenu />
    </MudDrawer>

    <MudDrawer @bind-Open="_chatDrawerOpen" Anchor="Anchor.End" Variant="DrawerVariant.Persistent" Elevation="2">
        <ClinicScheduler.Shared.Components.AgentChat />
    </MudDrawer>

    <MudMainContent role="main" id="main-content">
        <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="pa-4 pa-md-6">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private bool _drawerOpen = true;
    private bool _chatDrawerOpen = false;
}
